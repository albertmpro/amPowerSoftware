<c:Shell x:Class="abFlowDocs.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:Albert.Standard.Win32;assembly=AlbertWPFCore"
        xmlns:local="clr-namespace:abFlowDocs.View"
        mc:Ignorable="d"
        Title="abFlowDoc" Icon="/Assets/abmediaLogo.png" Height="600" Width="960" WindowState="Maximized">
    <Grid Background="{StaticResource amBackBrush}">
       <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <!-- TabControl-->
        <TabControl x:Name="tab" />
        <!-- Status Bar -->
        <StatusBar x:Name="status" Grid.Row="1" >
            <TextBlock Text="Status:" />
        </StatusBar>
    </Grid>
</c:Shell>
